# Includes and libs
INCDIR = -I/usr/local/include
INCCSDIR = ${INCDIR}/ClearSilver
LIBDIR = -L/usr/local/lib -L/usr/lib

# Install dir
PREFIX = /usr/local
BINDIR = ${PREFIX}/bin
ETCDIR = ${PREFIX}/etc
SHAREDIR = ${PREFIX}/share
